# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# Data / persistence
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2

# Audio synthesis for the built-in mock provider
numpy>=1.26

# HTTP client — used by the smoke test (FastAPI TestClient) and future
# HTTP-based providers (Mubert / Stable Audio hosted API).
httpx>=0.27
